/**
 * Declare variables
 * (see about scope coreJs_let_const_declarations.js)
 */

let a = 1;
const b = 2;
var c = 3;

let f1 = f2 = 5; // by value
let o1 = o2 = {}; // by reference


/**
 * Primitive data types
 */

// Boolean (Булев, Логический тип)
// Null (Null тип )
// Undefined (Неопределенный тип)
// Number (Число)
// String (Строка)
// Symbol (в ECMAScript 6)

/**
 * Literals
 * String, Number, Object, Array literals
 *
 * Array literals
 * Boolean literals
 * Floating-point literals
 * Integers
 * Object literals
 * RegExp literals
 * String literals
 */